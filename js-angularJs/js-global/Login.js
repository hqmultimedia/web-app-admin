upload.controller('LoginRegisterController', function($rootScope, $scope, $http, $location) {

        // //A. kiểm tra xem đăng nhập chưa nếu có thì chuyển thẳng đến trang home (Khi chưa đăng nhập)
        // if (!localStorage.getItem("data_Login")) {
        //     // gọi đến hàm bật tắt sidebar trong HQ-Js
        //     hidden_menu_footer();
        //     // $location.path('/login');

        // } else {

        //     $location.path('/home');

        // }








    }) //End controller LoginRegisterController